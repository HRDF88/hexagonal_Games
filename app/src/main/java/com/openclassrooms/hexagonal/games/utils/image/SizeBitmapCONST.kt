package com.openclassrooms.hexagonal.games.utils.image

/**
 * Utility object that defines constant values for bitmap size and quality.
 */
object SizeBitmapCONST {

    /**
     * Maximum height for the bitmap.
     */
    const val maxHeight: Int = 250

    /**
     * Maximum width for the bitmap.
     */
    const val maxWidth: Int = 250

    /**
     * Default quality for bitmap compression (0-100).
     */
    const val quality: Int = 100
}